(ns tictactoe.core-spec
  (:require [speclj.core :refer :all]
            [tictactoe.player :refer :all]))

; (describe "Player"

  ; (it "should return a number"
  ;   (should (= (tictactoe.player/number) 2)))
  ; 
  ; (it "is true"
  ;   (should (= (tictactoe.player/number) 1))))

(run-specs)